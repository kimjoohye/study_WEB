'use strict';

// Date객체
let dateObj = new Date();

let dateInfo = {
    year : dateObj.getFullYear(), // 년도 4자리
    month : dateObj.getMonth()+1 // 월 0~11 + 1필요
    day : dateObk.getDate() // 일(1~31)
};

// year : 2022
// month : 5
for(let i in dateInfo) {
    console.log(`${i} : ${dateInfo[i]}`);
}